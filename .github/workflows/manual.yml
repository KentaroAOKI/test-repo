name: Manual Trigger
on:
  workflow_dispatch:
    inputs:
      name:
        type: string
        description: 'Your name'
        required: true
        default: 'World'
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the user
        run: echo "Hello, ${{ github.event.inputs.name }}!"